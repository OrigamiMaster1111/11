#include <stdio.h>
#include <string.h>

void main()
{
    char str[100],i,j,k,c;
    printf("Please input a string:");
    gets(str);
    for (i=0;str[i]!='\0';i++)
    {
      for (j=k=i;str[j]!='\0';j++)
         if (str[k]>str[j])
/**********found**********/
       k=j;
      if (i!=k)
      {
         c=str[k];
/**********found**********/
         str[k]= str[i];
         str[i]=c;
      }
    }
    printf("sorted string:%s\n",str);
    k=0;
    for (i=1;str[i]!='\0';i++)
    {
       if (str[i]==str[i-1])
          k++;
       else
/**********found**********/
          str[i-k]=str[i];
    }
    str[strlen(str)-k]='\0';
    printf("deleted string:%s\n\n", str);
    getchar();
}






#include <stdio.h>

int fun(char *str)
{
   int temp=1, max=0;
   char c=' ';
   while (*str)
   {
      if (c == *str)
        temp++;
      else
      {
         c=*str;
/**********found**********/
         temp=1;
      }
/**********found**********/
      if (max < temp)
         max=temp;
      str++;
  }
/**********found**********/
   return max;
}

void main()
{
    char str[100];
    int max;
    printf("Please a string: ");
    gets(str);
    max = fun(str);
    printf("result=%d\n", max);
    getchar();
}
