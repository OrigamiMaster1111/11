int main()
{
	int a;//范围
	int n;//待判断数字
	int c;//除数

	scanf("%d", &a);

		for (n = 2;a >= n;n++) {
			int i = 1;//判断条件		
					for (c = 2;n > c;c++) {
								if (n % c == 0) {
									i = 0;
									break;
								}
					}
				if (i == 1) {
					printf("%d  ", n);
				}
		}
		return 0;
}
