#include <easyx.h>
#include <stdio.h>

int main(){
    int x = 250, y = 250;
    initgraph(x, y);
    
    for (int i = (x/2)-25; i <= (x/2)+25; i++){
        for (int j = (y/2)-25; j <= (y/2)+25; j++){
            putpixel(i, j, RED);       
        }
    }

    getchar();
    return 0;
}
